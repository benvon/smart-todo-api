apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: smart-todo

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - migration-job.yaml
  - deployment.yaml
  - service.yaml
